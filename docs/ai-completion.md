# 🤖 AI Автодополнение

Полное руководство по AI-автодополнению в bad4izNvim конфигурации.

---

## 📑 Содержание

- [Обзор](#обзор)
- [Supermaven](#supermaven)
  - [Inline подсказки](#inline-подсказки-серый-текст)
  - [В меню nvim-cmp](#в-меню-nvim-cmp-фиолетовый)
- [Codeium](#codeium)
- [nvim-cmp (общие команды)](#nvim-cmp-общие-команды)
- [Приоритеты источников](#приоритеты-источников)
- [Настройка и конфигурация](#настройка-и-конфигурация)
- [Советы по использованию](#советы-по-использованию)

---

## Обзор

В bad4izNvim настроено два AI-движка для автодополнения:

1. **Supermaven** - основной AI-помощник
2. **Codeium** - дополнительный AI-помощник

Оба работают одновременно и предлагают подсказки через:
- **Inline режим** - серый текст прямо в коде (только Supermaven)
- **Меню nvim-cmp** - выпадающее меню с подсказками (оба)

---

## Supermaven

Supermaven - мощный AI-помощник, работающий в двух режимах одновременно.

### Inline подсказки (серый текст)

Подсказки появляются автоматически во время набора текста серым цветом.

| Команда                       | Хоткей       | Описание |
| ----------------------------- | ------------ | -------- |
| Принять подсказку             | `<C-g>`      | Вставить весь предложенный текст |
| Принять слово                 | `<C-Right>`  | Вставить только следующее слово |
| Очистить подсказку            | `<C-]>`      | Убрать текущую подсказку |

**Пример работы:**
```javascript
// Вы набираете:
function calculate

// Supermaven предлагает (серым):
function calculateTotal(items) {
  return items.reduce((sum, item) => sum + item.price, 0);
}

// Нажмите Ctrl+g, чтобы принять
```

### В меню nvim-cmp (фиолетовый)

Supermaven также интегрирован в меню автодополнения nvim-cmp.

**Идентификация:**
- Источник помечен как `[Supermaven]`
- Цвет: фиолетовый (#d787ff)
- Иконка: AI символ

**Приоритет:** Высокий (1010) - подсказки Supermaven в меню появляются первыми

---

## Codeium

Codeium - дополнительный AI-помощник, работающий через меню nvim-cmp.

### В меню nvim-cmp (бирюзовый)

| Идентификация | Значение |
| ------------- | -------- |
| Источник | `[Codeium]` |
| Цвет | Бирюзовый (#00d7af) |
| Иконка | AI символ |
| Приоритет | 1010 |

**Особенности:**
- Работает только через меню автодополнения
- Предлагает альтернативные варианты к Supermaven
- Хорошо дополняет Supermaven разными подходами

---

## nvim-cmp (общие команды)

nvim-cmp - система автодополнения, которая объединяет все источники.

### Основные команды

| Команда                       | Хоткей       | Описание |
| ----------------------------- | ------------ | -------- |
| Принять выбранную подсказку   | `<CR>` (Enter) | Вставить выбранный элемент |
| Следующая подсказка           | `<C-n>`      | Вниз по списку |
| Предыдущая подсказка          | `<C-p>`      | Вверх по списку |
| Открыть меню                  | `<C-Space>`  | Показать автодополнение |
| Закрыть меню                  | `<C-e>`      | Скрыть меню |

### Навигация в меню

| Команда                       | Хоткей       | Описание |
| ----------------------------- | ------------ | -------- |
| Вниз по списку                | `<C-n>` или `<Down>` | Следующий элемент |
| Вверх по списку               | `<C-p>` или `<Up>` | Предыдущий элемент |
| Скролл документации вниз      | `<C-d>`      | Scroll docs down |
| Скролл документации вверх     | `<C-u>`      | Scroll docs up |

### Дополнительные функции

| Команда                       | Хоткей       | Описание |
| ----------------------------- | ------------ | -------- |
| Развернуть сниппет            | `<Tab>`      | Если выбран snippet |
| Следующая позиция в сниппете  | `<Tab>`      | В режиме snippet |
| Предыдущая позиция в сниппете | `<S-Tab>`    | В режиме snippet |

---

## Приоритеты источников

Источники автодополнения отображаются в меню в следующем порядке:

| Приоритет | Источник | Описание | Цвет |
| --------- | -------- | -------- | ---- |
| 1010 | Supermaven | AI подсказки | Фиолетовый #d787ff |
| 1010 | Codeium | AI подсказки | Бирюзовый #00d7af |
| 1000 | LSP | Language Server | Стандартный |
| 750 | LuaSnip | Сниппеты | Стандартный |
| 700 | Emoji | Эмодзи `:smile:` | Стандартный |
| 500 | Buffer | Слова из буфера | Стандартный |
| 250 | Path | Пути к файлам | Стандартный |

**Примечание:** Более высокий приоритет означает, что источник появляется выше в списке.

---

## Настройка и конфигурация

### Включение/выключение AI помощников

#### Supermaven

```vim
" Переключить Supermaven
:SupermavenToggle

" Выключить Supermaven
:SupermavenStop

" Включить Supermaven
:SupermavenStart

" Показать статус
:SupermavenStatus
```

#### Codeium

Codeium настроен как источник в nvim-cmp и не требует отдельного управления.

### Файлы конфигурации

| Плагин | Файл конфигурации |
| ------ | ----------------- |
| Supermaven | `lua/plugins/supermaven.lua` |
| Codeium | `lua/plugins/codeium-cmp.lua` |
| nvim-cmp | `lua/plugins/cmp.lua` |

---

## Советы по использованию

### 1. Комбинируйте режимы

**Inline режим (Supermaven):**
- ✅ Лучше для быстрого написания простого кода
- ✅ Не отвлекает от процесса
- ✅ Хорошо для автодополнения больших блоков

**Меню режим (nvim-cmp):**
- ✅ Лучше для выбора из вариантов
- ✅ Видно все источники (AI, LSP, snippets)
- ✅ Можно сравнить предложения

### 2. Принимайте по словам

Вместо принятия всей подсказки целиком (`Ctrl+g`), принимайте по одному слову (`Ctrl+Right`).

**Преимущества:**
- Больше контроля
- Можно корректировать на ходу
- Лучше понимание кода

### 3. Используйте Ctrl+Space для вызова

Если автодополнение не появилось автоматически, вызовите его вручную:
```
Ctrl+Space - показать все варианты
```

### 4. LSP + AI = отличная комбинация

- LSP даёт точные предложения для вашего проекта
- AI предлагает креативные решения
- Вместе покрывают все случаи

### 5. Работа со сниппетами

LuaSnip интегрирован с nvim-cmp:
1. Начните набирать триггер сниппета
2. Выберите в меню (помечено `[LuaSnip]`)
3. `Enter` для развертывания
4. `Tab` / `Shift+Tab` для навигации по позициям

### 6. Отключение в определенных ситуациях

Если AI мешает (например, при редактировании конфига):
```vim
:SupermavenToggle  " Временно выключить
```

### 7. Сравнение предложений AI

Когда и Supermaven, и Codeium предлагают разные варианты:
1. Откройте меню (`Ctrl+Space`)
2. Посмотрите оба варианта
3. Выберите лучший (`Ctrl+n`/`Ctrl+p` + `Enter`)

---

## Частые вопросы

### Q: Почему появляется несколько одинаковых подсказок?

A: Это разные источники (Supermaven и Codeium) предлагают похожие варианты. Выбирайте любой.

### Q: Как узнать, откуда подсказка?

A: Смотрите на:
- Метку в квадратных скобках: `[Supermaven]`, `[Codeium]`, `[LSP]`
- Цвет: фиолетовый (Supermaven), бирюзовый (Codeium)

### Q: Можно ли использовать только один AI?

A: Да, можно отключить ненужный в конфигурации или закомментировать плагин.

### Q: Подсказки не появляются

A: Проверьте:
1. `:SupermavenStatus` - работает ли Supermaven
2. `:CmpStatus` - состояние nvim-cmp
3. Интернет-соединение (AI требует подключения)

### Q: Как ускорить появление подсказок?

A: Подсказки появляются автоматически. Если хотите вызвать немедленно - `Ctrl+Space`.

---

## Цветовая схема источников

В меню nvim-cmp источники выделены цветом для быстрой идентификации:

```
🟣 Supermaven  (#d787ff) - AI подсказки
🔵 Codeium    (#00d7af) - AI подсказки
⚪ LSP        (default) - языковой сервер
🟡 LuaSnip    (default) - сниппеты
⚫ Buffer     (default) - слова из файла
⚫ Path       (default) - пути к файлам
⚫ Emoji      (default) - эмодзи
```

---

## Дополнительные ресурсы

- [Supermaven документация](https://supermaven.com/docs)
- [Codeium документация](https://codeium.com/docs)
- [nvim-cmp документация](https://github.com/hrsh7th/nvim-cmp)
- [LuaSnip документация](https://github.com/L3MON4D3/LuaSnip)
- [Конфигурация автодополнения](../lua/plugins/cmp.lua)
