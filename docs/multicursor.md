# 🎯 Множественные курсоры (Multiple Cursors)

Плагин **vim-visual-multi** - мощный инструмент для одновременного редактирования нескольких мест в коде. Это аналог функции Ctrl+D в VSCode, но с гораздо большими возможностями.

---

## 📑 Содержание

- [Основные команды](#основные-команды)
- [Кейсы использования](#кейсы-использования)
  - [Кейс 1: Переименование переменной](#кейс-1-переименование-переменной)
  - [Кейс 2: Множественные курсоры вниз](#кейс-2-множественные-курсоры-вниз)
  - [Кейс 3: Редактирование списка](#кейс-3-редактирование-списка)
  - [Кейс 4: Добавление точки с запятой](#кейс-4-добавление-точки-с-запятой)
  - [Кейс 5: Работа с массивом объектов](#кейс-5-работа-с-массивом-объектов)
  - [Кейс 6: Конвертация CSS в JS объект](#кейс-6-конвертация-css-в-js-объект)
  - [Кейс 7: Добавление импортов](#кейс-7-добавление-импортов)
- [Режимы работы](#режимы-работы)
- [Советы и трюки](#советы-и-трюки)

---

## Основные команды

### Выделение и создание курсоров

| Команда | Хоткей | Описание |
|---------|--------|----------|
| Выделить слово и добавить следующее | `Ctrl+n` | Аналог Ctrl+D в VSCode |
| Добавить курсор ниже | `Ctrl+Down` | Создать курсор на следующей строке |
| Добавить курсор выше | `Ctrl+Up` | Создать курсор на предыдущей строке |
| Выделить все вхождения | `Ctrl+n` (многократно) | Нажимайте до выделения всех |
| Создать курсоры из визуального режима | `V` + `jjj` + `Ctrl+n` | Выделить строки, затем Ctrl+n |

### Навигация между курсорами

| Команда | Хоткей | Описание |
|---------|--------|----------|
| Следующий курсор | `n` | В режиме multiple cursors |
| Предыдущий курсор | `N` | В режиме multiple cursors |
| Пропустить текущий | `q` | Пропустить и перейти к следующему |
| Удалить текущий курсор | `Q` | Убрать один курсор |

### Режимы редактирования

| Команда | Хоткей | Описание |
|---------|--------|----------|
| Insert mode | `i` | Вставка перед курсором |
| Append mode | `a` | Вставка после курсора |
| Change mode | `c` | Удалить и начать редактирование |
| Insert в начале строки | `I` | Все курсоры в начало строк |
| Append в конце строки | `A` | Все курсоры в конец строк |
| Переключить режим | `Tab` | Cursor ↔ Extend mode |

### Выход из режима

| Команда | Хоткей | Описание |
|---------|--------|----------|
| Выход | `Esc` | Применить изменения и выйти |
| Отмена | `Ctrl+c` | Отменить и выйти |

---

## Кейсы использования

### Кейс 1: Переименование переменной

**Задача:** Переименовать `userName` в `currentUser` во всем файле

**Исходный код:**
```javascript
const userName = "John";
console.log(userName);
alert(userName);
setUser(userName);
```

**Действия:**
1. Курсор на `userName`
2. `Ctrl+n` → `Ctrl+n` → `Ctrl+n` → `Ctrl+n` (выделить все 4 вхождения)
3. `c` (change) → наберите `currentUser`
4. `Esc`

**Результат:**
```javascript
const currentUser = "John";
console.log(currentUser);
alert(currentUser);
setUser(currentUser);
```

---

### Кейс 2: Множественные курсоры вниз

**Задача:** Добавить `const` перед каждой переменной

**Исходный код:**
```javascript
name = "John"
age = 30
city = "New York"
country = "USA"
```

**Действия (способ 1 - Ctrl+Down):**
1. Курсор на начало первой строки
2. `Ctrl+Down` → `Ctrl+Down` → `Ctrl+Down` (3 раза)
3. `I` (Insert в начале строки)
4. Наберите `const `
5. `Esc`

**Действия (способ 2 - через Visual mode):**
1. Курсор на первой строке
2. `V` (Visual Line mode)
3. `jjj` (выделить 4 строки)
4. `Ctrl+n`
5. `I` → наберите `const `
6. `Esc`

**Результат:**
```javascript
const name = "John"
const age = 30
const city = "New York"
const country = "USA"
```

---

### Кейс 3: Редактирование списка

**Задача:** Превратить список в массив строк

**Исходный код:**
```
apple
banana
orange
grape
```

**Действия:**
1. `V` → `jjj` (выделить все строки)
2. `Ctrl+n`
3. `I` → наберите `  "`
4. `Esc`
5. `A` → наберите `",`
6. `Esc`

**Результат:**
```javascript
  "apple",
  "banana",
  "orange",
  "grape",
```

---

### Кейс 4: Добавление точки с запятой

**Задача:** Добавить точку с запятой в конец строк

**Исходный код:**
```javascript
const a = 1
const b = 2
const c = 3
```

**Действия:**
1. `V` → `jj` (выделить строки)
2. `Ctrl+n`
3. `A` → наберите `;`
4. `Esc`

**Результат:**
```javascript
const a = 1;
const b = 2;
const c = 3;
```

---

### Кейс 5: Работа с массивом объектов

**Задача:** Добавить поле `isActive: true` к каждому объекту

**Исходный код:**
```javascript
const users = [
  { id: 1, name: "John" },
  { id: 2, name: "Jane" },
  { id: 3, name: "Bob" },
];
```

**Действия:**
1. Курсор на `name` в первом объекте
2. `Ctrl+n` → `Ctrl+n` (выделить все `name`)
3. `f"` (перейти к кавычке после name)
4. `A` → наберите `, isActive: true`
5. `Esc`

**Результат:**
```javascript
const users = [
  { id: 1, name: "John", isActive: true },
  { id: 2, name: "Jane", isActive: true },
  { id: 3, name: "Bob", isActive: true },
];
```

---

### Кейс 6: Конвертация CSS в JS объект

**Задача:** Превратить CSS свойства в JS объект

**Исходный код:**
```css
color: red
font-size: 16px
margin: 10px
padding: 20px
```

**Действия:**
1. `V` → `jjj` (выделить все строки)
2. `Ctrl+n`
3. `I` → наберите `  `
4. `Esc`
5. Курсор на первое двоеточие
6. `Ctrl+n` → `Ctrl+n` → `Ctrl+n` (выделить все двоеточия)
7. `r'` (replace на одинарную кавычку) - заменит `:` на `'`
8. Ручная корректировка для завершения

**Альтернативный подход:**
1. Использовать поиск и замену: `:%s/\(.*\): \(.*\)/  '\1': '\2',/g`

---

### Кейс 7: Добавление импортов

**Задача:** Обернуть названия компонентов в импорты

**Исходный код:**
```
Button
Input
Card
Modal
```

**Действия:**
1. `V` → `jjj`
2. `Ctrl+n`
3. `I` → наберите `import `
4. `Esc`
5. `A` → наберите ` from './components/`
6. `Esc`
7. Ручное добавление кавычек и расширений

**Результат (после ручной доработки):**
```javascript
import Button from './components/Button'
import Input from './components/Input'
import Card from './components/Card'
import Modal from './components/Modal'
```

---

## Режимы работы

### Cursor Mode (режим курсора)
- Курсоры находятся в точках
- Используйте обычные Vim команды: `i`, `a`, `c`, `d`, и т.д.

### Extend Mode (режим расширения)
- Курсоры превращаются в выделения
- Можно расширять выделение с помощью движений Vim
- Переключение: `Tab`

---

## Советы и трюки

### 1. Если Ctrl+Down не работает
В некоторых терминалах комбинация `Ctrl+Down` может не работать из-за конфликта с системными хоткеями.

**Решение:**
Используйте Visual mode:
```
V → jjj → Ctrl+n
```

### 2. Выделить все вхождения в файле
1. Курсор на слово
2. `Ctrl+n` многократно ИЛИ
3. Используйте regex поиск: `/*` для выделения всех вхождений

### 3. Пропустить ненужное вхождение
Если при нажатии `Ctrl+n` выделилось слово, которое не нужно:
- Нажмите `q` (skip) - пропустит текущее и перейдет к следующему

### 4. Комбинация с Vim движениями
После создания курсоров можно использовать любые Vim движения:
- `w` - следующее слово
- `e` - конец слова
- `$` - конец строки
- `0` - начало строки
- `f<char>` - найти символ

### 5. Работа с регистрами
Можно копировать/вставлять в/из регистров:
1. Выделите с multiple cursors
2. `"ay` - копировать в регистр 'a'
3. `"ap` - вставить из регистра 'a'

### 6. Undo/Redo работает корректно
- `u` - отменить последнее изменение со всеми курсорами
- `Ctrl+r` - повторить

---

## Горячие клавиши по категориям

### Быстрая справка

#### Создание курсоров
```
Ctrl+n          - следующее вхождение слова
Ctrl+Down/Up    - курсор выше/ниже
V + jjj + Ctrl+n - курсоры из выделения
```

#### Редактирование
```
i/a/c/d         - обычные Vim команды
I/A             - начало/конец строки
Tab             - переключить режим
```

#### Навигация
```
n/N             - следующий/предыдущий курсор
q               - пропустить
Q               - удалить курсор
```

#### Выход
```
Esc             - применить и выйти
Ctrl+c          - отменить и выйти
```

---

## Дополнительные ресурсы

- [Официальная документация vim-visual-multi](https://github.com/mg979/vim-visual-multi)
- [Конфигурация плагина](../lua/plugins/multicursor.lua)

---

## Заключение

Множественные курсоры - это мощный инструмент, который значительно ускоряет рутинные задачи редактирования. С практикой вы будете использовать их автоматически для:

- Переименования переменных
- Форматирования списков
- Массового редактирования
- Рефакторинга кода

**Главное:** Начните с простых случаев (`Ctrl+n` для переименования) и постепенно осваивайте более сложные техники!
