# 📋 Quickfix Window (nvim-bqf)

Полное руководство по работе с Quickfix Window и плагином nvim-bqf.

---

## 📑 Содержание

- [Что такое Quickfix?](#что-такое-quickfix)
- [Открытие Quickfix](#открытие-quickfix)
- [Навигация и превью](#навигация-и-превью)
- [Действия с файлами](#действия-с-файлами)
- [Фильтрация (fzf)](#фильтрация-fzf)
- [Управление списком](#управление-списком)
- [Кейсы использования](#кейсы-использования)
- [Советы и трюки](#советы-и-трюки)

---

## Что такое Quickfix?

**Quickfix** - это специальное окно в Vim/Neovim для отображения списков:
- Результатов поиска (grep)
- Ошибок компиляции
- Результатов lint/проверок
- Списка файлов

**nvim-bqf** (Better Quickfix) - плагин, который значительно улучшает работу с Quickfix:
- Добавляет превью файлов
- Интеграция с fzf для фильтрации
- Удобная навигация
- Подсветка синтаксиса в превью

---

## Открытие Quickfix

### Автоматическое открытие

Quickfix открывается автоматически после команд:
```vim
:grep pattern **/*.js    " Поиск в файлах
:make                    " Сборка проекта
:Telescope live_grep     " Telescope search → Send to QF
```

### Ручное открытие

| Команда | Описание |
| ------- | -------- |
| `:copen` | Открыть quickfix window |
| `:cclose` | Закрыть quickfix window |
| `:cw` | Открыть если есть ошибки |

---

## Навигация и превью

### Основная навигация

| Команда                           | Хоткей      | Описание |
| --------------------------------- | ----------- | -------- |
| Следующий элемент                 | `j`         | Вниз по списку |
| Предыдущий элемент                | `k`         | Вверх по списку |
| Переключить превью                | `p`         | Показать/скрыть превью файла |
| Выбрать и следующий               | `<Tab>`     | Выбрать + перейти к следующему |
| Выбрать и предыдущий              | `<S-Tab>`   | Выбрать + перейти к предыдущему |

### Режимы превью

| Команда                           | Хоткей      | Описание |
| --------------------------------- | ----------- | -------- |
| Переключить режим превью          | `z,`        | Автопревью вкл/выкл |
| Скролл превью вниз                | `<C-d>`     | Scroll down |
| Скролл превью вверх               | `<C-u>`     | Scroll up |

**Режим автопревью:**
- Включен: превью обновляется при перемещении по списку
- Выключен: превью показывается только при нажатии `p`

---

## Действия с файлами

### Открытие файлов

| Команда                            | Хоткей      | Описание |
| ---------------------------------- | ----------- | -------- |
| Открыть (quickfix остается)        | `<CR>` (Enter) | Открыть в последнем окне |
| Открыть и закрыть quickfix         | `o`         | Открыть и закрыть QF |
| Открыть в вертикальном сплите      | `<C-v>`     | Vertical split |
| Открыть в горизонтальном сплите    | `<C-x>`     | Horizontal split |
| Открыть в новой вкладке            | `<C-t>`     | New tab |

**Разница между `Enter` и `o`:**
- `Enter` - quickfix окно остается открытым
- `o` - quickfix закрывается автоматически

---

## Фильтрация (fzf)

nvim-bqf интегрирован с fzf для мощной фильтрации списка.

### Основные команды фильтрации

| Команда                            | Хоткей      | Описание |
| ---------------------------------- | ----------- | -------- |
| Открыть fzf фильтр                 | `zf`        | Фильтровать видимые элементы |
| Открыть fzf (со скрытыми)          | `zF`        | Фильтровать все (включая скрытые) |

### В режиме fzf

После нажатия `zf` или `zF`:

| Команда                            | Хоткей      | Описание |
| ---------------------------------- | ----------- | -------- |
| Фильтровать                        | Набирайте текст | Fuzzy поиск |
| Открыть выбранное                  | `<CR>`      | Открыть файл |
| Открыть в сплите                   | `<C-s>`     | Split |
| Открыть в вертикальном сплите      | `<C-v>`     | Vertical split |
| Выбрать все                        | `<C-o>`     | Select all |
| Выбрать несколько                  | `<Tab>`     | Toggle selection |
| Выход                              | `<Esc>`     | Отмена |

---

## Управление списком

### Базовые команды Quickfix

| Команда | Описание |
| ------- | -------- |
| `:cnext` или `:cn` | Следующий элемент |
| `:cprevious` или `:cp` | Предыдущий элемент |
| `:cfirst` | Первый элемент |
| `:clast` | Последний элемент |
| `:colder` | Предыдущий список QF |
| `:cnewer` | Следующий список QF |

### История Quickfix

Neovim хранит историю списков quickfix:
```vim
:colder    " Вернуться к предыдущему списку
:cnewer    " Вернуться к следующему списку
:chistory  " Показать историю списков
```

---

## Кейсы использования

### Кейс 1: Поиск и замена по проекту

**Задача:** Найти все вхождения `oldFunction` и заменить на `newFunction`

**Шаги:**
1. `:grep oldFunction **/*.js` - найти все вхождения
2. Quickfix откроется автоматически
3. `zf` - открыть fzf для фильтрации (опционально)
4. `p` - просмотреть файл перед открытием
5. `<CR>` - открыть файл
6. Сделать замену: `:%s/oldFunction/newFunction/g`
7. Повторить для других файлов

**Автоматизация:**
```vim
:cfdo %s/oldFunction/newFunction/ge | update
```
Эта команда заменит во всех файлах из quickfix списка.

### Кейс 2: Исправление ошибок линтера

**Задача:** Исправить все ошибки ESLint в проекте

**Шаги:**
1. `:make` или запустить линтер
2. Ошибки появятся в quickfix
3. `zf` → введите тип ошибки для фильтрации
4. `<Tab>` - выбрать и перейти к следующему
5. Исправить ошибку
6. `<Tab>` - следующая ошибка

### Кейс 3: Просмотр результатов Telescope

**Задача:** Найти через Telescope и отправить в quickfix для удобной работы

**Шаги:**
1. `<Leader>fw` - Telescope live grep
2. В Telescope: `<C-q>` - отправить результаты в quickfix
3. Quickfix откроется со всеми результатами
4. `p` - превью
5. `<Tab>` - навигация с предпросмотром

### Кейс 4: Работа с несколькими файлами

**Задача:** Открыть несколько файлов для редактирования

**Шаги:**
1. `:grep pattern **/*.js`
2. `zf` - фильтровать нужные файлы
3. `<C-o>` - выбрать все
4. `<CR>` - открыть все в буферах
5. `:bufdo %s/old/new/ge | update` - массовая замена

---

## Советы и трюки

### 1. Использование превью

**Режим автопревью (`z,`):**
- ✅ Удобно для быстрого просмотра
- ❌ Может тормозить на больших файлах

**Ручной превью (`p`):**
- ✅ Больше контроля
- ✅ Не тормозит

### 2. Комбинация Tab + Enter

Workflow для быстрой навигации:
1. `<Tab>` - просмотреть несколько файлов
2. `<CR>` - открыть нужный

### 3. Фильтрация перед работой

Перед массовыми операциями:
1. `zf` - отфильтровать список
2. Проверить что все нужное выбрано
3. Выполнить команду (`:cfdo`, `:cdo`)

### 4. Быстрый переход

Вместо навигации в quickfix, используйте:
```vim
:cn  " Следующий
:cp  " Предыдущий
```

Можно назначить хоткеи:
```lua
vim.keymap.set('n', ']q', ':cnext<CR>')
vim.keymap.set('n', '[q', ':cprev<CR>')
```

### 5. Сохранение списка quickfix

Сохранить список в файл:
```vim
:w quickfix.txt
```

### 6. Загрузка списка в quickfix

Загрузить файл в quickfix:
```vim
:cfile errors.txt
:cgetfile errors.txt
```

---

## Команды для массовых операций

### cfdo vs cdo

**`:cfdo` (file do):**
```vim
:cfdo %s/old/new/ge | update
```
Выполняет команду для каждого **файла** в списке (один раз на файл).

**`:cdo` (do):**
```vim
:cdo s/old/new/ge | update
```
Выполняет команду для каждой **строки** в списке.

### Примеры массовых операций

**Заменить во всех файлах:**
```vim
:cfdo %s/oldText/newText/ge | update
```

**Добавить строку в начало всех файлов:**
```vim
:cfdo 1put! ='// Header' | update
```

**Удалить пустые строки во всех файлах:**
```vim
:cfdo g/^$/d | update
```

**Запустить команду для всех файлов:**
```vim
:cfdo !eslint --fix %
```

---

## Интеграция с другими инструментами

### Telescope → Quickfix

В Telescope используйте:
- `<C-q>` - отправить выбранное в quickfix
- `<M-q>` - отправить все в quickfix

### Trouble → Quickfix

Trouble может отправлять свои списки в quickfix для дальнейшей обработки.

### LSP → Quickfix

Результаты LSP (диагностика, ссылки) можно отправить в quickfix:
```vim
:lua vim.diagnostic.setqflist()
```

---

## Горячие клавиши (справка)

### В Quickfix окне

```
j/k             - навигация
p               - превью
Tab/S-Tab       - выбрать и след/пред
Enter           - открыть (QF остается)
o               - открыть (QF закрыть)
C-v             - vertical split
C-x             - horizontal split
C-t             - new tab
zf              - fzf фильтр
zF              - fzf (все)
z,              - переключить автопревью
q               - закрыть quickfix
```

### Команды в command mode

```
:copen          - открыть QF
:cclose         - закрыть QF
:cn / :cnext    - следующий
:cp / :cprev    - предыдущий
:cfirst         - первый
:clast          - последний
:colder         - предыдущий список
:cnewer         - следующий список
```

---

## Дополнительные ресурсы

- [nvim-bqf документация](https://github.com/kevinhwang91/nvim-bqf)
- [Quickfix в Vim](https://vimhelp.org/quickfix.txt.html)
- [Конфигурация nvim-bqf](../lua/plugins/nvim-bqf.lua)
